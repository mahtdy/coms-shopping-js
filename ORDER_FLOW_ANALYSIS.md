# 📊 تحلیل جریان سفارش - فاز 2

**تاریخ:** 2025  
**هدف:** بررسی کامل جریان سفارش از افزودن کالا به سبد تا ثبت سفارش و صدور صورتحساب

---

## 📋 فهرست مطالب

1. [جریان فعلی سفارش](#جریان-فعلی-سفارش)
2. [نقاط خالی و نیازمندی‌ها](#نقاط-خالی-و-نیازمندیها)
3. [اولویت‌بندی بهبودها](#اولویتبندی-بهبودها)
4. [پیشنهادات پیاده‌سازی](#پیشنهادات-پیادهسازی)

---

## جریان فعلی سفارش

### ✅ مرحله 1: افزودن کالا به سبد

**کنترلر:** `BasketController` (`src/apps/user/controllers/basket.ts`)

**متد:** `create()`

**عملکرد:**
- ✅ بررسی وجود سبد برای کاربر
- ✅ ایجاد سبد جدید یا به‌روزرسانی سبد موجود
- ✅ بررسی موجودی محصول در انبار
- ✅ محاسبه قیمت محصول
- ✅ افزودن محصول به `basketList`

**نکات:**
- اگر محصول قبلاً در سبد باشد، تعداد آن افزایش می‌یابد
- موجودی انبار بررسی می‌شود

---

### ✅ مرحله 2: مدیریت سبد

**متدها:**
- `POST /user/basket/increase` - افزایش تعداد
- `POST /user/basket/decrease` - کاهش تعداد

**عملکرد:**
- ✅ بررسی موجودی قبل از افزایش
- ✅ حذف خودکار محصول در صورت رسیدن تعداد به صفر

---

### ✅ مرحله 3: Checkout (تسویه حساب)

**کنترلر:** `BasketController` (`POST /user/basket/checkout`)

**جریان:**
```
1. دریافت سبد کاربر
   ↓
2. فراخوانی BasketOrderService.checkoutFromBasket()
   ↓
3. اعتبارسنجی موجودی محصولات
   ↓
4. محاسبه قیمت کل محصولات
   ↓
5. ثبت سفارش در دیتابیس
   ↓
6. کاهش موجودی از انبار
   ↓
7. خالی کردن سبد
   ↓
8. ایجاد پرداخت
   ↓
9. ایجاد بسته ارسالی (در صورت وجود آدرس)
   ↓
10. بازگشت نتیجه
```

**ورودی‌ها (CheckoutMeta):**
- `address`: آدرس ارسال (اختیاری)
- `offCode`: کد تخفیف (اختیاری) - **⚠️ استفاده نمی‌شود**
- `sendType`: نوع ارسال (1, 2, ...) - **⚠️ استفاده نمی‌شود**
- `sendTime`: زمان ارسال - **⚠️ استفاده نمی‌شود**
- `sendDate`: تاریخ ارسال - **⚠️ استفاده نمی‌شود**
- `isBig`: بسته بزرگ - **⚠️ استفاده نمی‌شود**
- `typePeyment`: نوع پرداخت - **⚠️ استفاده نمی‌شود**

---

### ✅ مرحله 4: ثبت سفارش

**سرویس:** `BasketOrderService.createOrderTransaction()`

**عملکرد:**
- ✅ اعتبارسنجی لیست محصولات
- ✅ بررسی و اتصال آدرس
- ✅ محاسبه قیمت کل محصولات (`totalPriceProducts`)
- ✅ محاسبه هزینه کل (`totalCost`)
- ✅ ثبت سفارش در دیتابیس
- ✅ کاهش موجودی از انبار

**فیلدهای Order:**
- `user`: کاربر
- `orderList`: لیست محصولات
- `totalCost`: هزینه کل (خرید)
- `totalPriceProducts`: قیمت کل محصولات
- `address`: آدرس ارسال
- `package`: بسته ارسالی
- `deliveryStatus`: وضعیت ارسال

---

### ✅ مرحله 5: پرداخت

**سرویس:** `PaymentService.initiatePayment()`

**عملکرد:**
- ✅ ایجاد PaymentIntent
- ✅ در حال حاضر از MockPaymentGateway استفاده می‌کند
- ✅ آماده برای اتصال به درگاه واقعی

---

### ✅ مرحله 6: ایجاد بسته ارسالی

**سرویس:** `DeliveryService.createPackageFromOrder()`

**عملکرد:**
- ✅ ایجاد بسته از سفارش
- ✅ تولید کد رهگیری
- ✅ اتصال به سفارش

---

### ✅ مرحله 7: تخفیف پس از فاکتور

**کنترلر:** `DiscountController.generateDiscountAfterInvoice()`

**عملکرد:**
- ✅ تولید کد تخفیف پس از ثبت سفارش
- ✅ انواع تخفیف: Fixed, Random, Percent
- ✅ محدودیت‌های مختلف

**نکته:** این متد در کنترلر Order (admin) فراخوانی می‌شود، اما در checkout کاربر فراخوانی نمی‌شود.

---

## نقاط خالی و نیازمندی‌ها

### 🔴 اولویت بالا

#### 1. محاسبه هزینه ارسال (Shipping Cost)

**وضعیت:** ❌ پیاده‌سازی نشده

**مشکل:**
- فیلدهای `sendType`, `sendTime`, `sendDate`, `isBig` در `CheckoutMeta` وجود دارند
- اما هزینه ارسال محاسبه نمی‌شود
- در `totalPriceProducts` فقط قیمت محصولات است

**نیازمندی:**
- سرویس محاسبه هزینه ارسال بر اساس:
  - نوع ارسال (`sendType`)
  - زمان ارسال (`sendTime`)
  - وزن/حجم بسته (`isBig`)
  - فاصله (از آدرس مبدا تا مقصد)
- افزودن فیلد `shippingCost` به مدل Order
- افزودن به محاسبه نهایی

**فایل‌های نیازمند تغییر:**
- `src/apps/services/basketOrderService.ts`
- `src/repositories/admin/order/model.ts`
- ایجاد `src/apps/services/shippingService.ts`

---

#### 2. اعمال کد تخفیف در Checkout

**وضعیت:** ❌ پیاده‌سازی نشده

**مشکل:**
- فیلد `offCode` در `CheckoutMeta` وجود دارد
- اما در checkout اعمال نمی‌شود
- تخفیف فقط پس از فاکتور تولید می‌شود

**نیازمندی:**
- اعتبارسنجی کد تخفیف (`offCode`)
- محاسبه مقدار تخفیف
- اعمال تخفیف به قیمت کل
- افزودن فیلد `discountAmount` به Order
- ثبت کد تخفیف استفاده شده

**فایل‌های نیازمند تغییر:**
- `src/apps/services/basketOrderService.ts`
- `src/repositories/admin/order/model.ts`
- استفاده از `DiscountRepository` برای اعتبارسنجی

---

#### 3. محاسبه مالیات (Tax)

**وضعیت:** ❌ پیاده‌سازی نشده

**مشکل:**
- مالیات محاسبه نمی‌شود
- در صورتحساب نهایی وجود ندارد

**نیازمندی:**
- سرویس محاسبه مالیات
- نرخ مالیات (مثلاً 9% برای ارزش افزوده)
- افزودن فیلد `taxAmount` به Order
- افزودن به محاسبه نهایی

**فایل‌های نیازمند تغییر:**
- `src/apps/services/basketOrderService.ts`
- `src/repositories/admin/order/model.ts`
- ایجاد `src/apps/services/taxService.ts`

---

#### 4. شماره فاکتور/سفارش

**وضعیت:** ❌ پیاده‌سازی نشده

**مشکل:**
- Order فقط `_id` دارد
- شماره فاکتور قابل خواندن وجود ندارد

**نیازمندی:**
- افزودن فیلد `orderNumber` یا `invoiceNumber` به Order
- تولید خودکار شماره فاکتور (مثلاً ORD-2025-0001)
- استفاده از `InvoiceRepository.getFactorNumber()` که در core وجود دارد

**فایل‌های نیازمند تغییر:**
- `src/repositories/admin/order/model.ts`
- `src/apps/services/basketOrderService.ts`

---

#### 5. محاسبه نهایی (Final Total)

**وضعیت:** ⚠️ ناقص

**مشکل:**
- فقط `totalPriceProducts` و `totalCost` محاسبه می‌شود
- هزینه ارسال، مالیات، و تخفیف در محاسبه نهایی نیست

**نیازمندی:**
- محاسبه:
  ```
  finalTotal = totalPriceProducts 
               - discountAmount 
               + shippingCost 
               + taxAmount
  ```
- افزودن فیلد `finalTotal` به Order
- بازگشت محاسبه نهایی در response

**فایل‌های نیازمند تغییر:**
- `src/apps/services/basketOrderService.ts`
- `src/repositories/admin/order/model.ts`

---

### 🟡 اولویت متوسط

#### 6. وضعیت سفارش (Order Status)

**وضعیت:** ⚠️ ناقص

**مشکل:**
- فقط `deliveryStatus` وجود دارد
- وضعیت کلی سفارش (pending, confirmed, processing, completed, cancelled) وجود ندارد

**نیازمندی:**
- افزودن فیلد `orderStatus` به Order
- مدیریت تغییرات وضعیت
- تاریخچه تغییرات وضعیت

**فایل‌های نیازمند تغییر:**
- `src/repositories/admin/order/model.ts`
- ایجاد `src/apps/services/orderStatusService.ts`

---

#### 7. فاکتور/صورتحساب (Invoice)

**وضعیت:** ⚠️ ناقص

**مشکل:**
- `InvoiceRepository` در `src/core` وجود دارد
- اما به Order متصل نیست
- فاکتور برای Order ایجاد نمی‌شود

**نیازمندی:**
- ایجاد فاکتور پس از ثبت سفارش
- اتصال Invoice به Order
- استفاده از `InvoiceRepository` موجود

**فایل‌های نیازمند تغییر:**
- `src/apps/services/basketOrderService.ts`
- استفاده از `InvoiceRepository` از core

---

#### 8. هزینه بسته‌بندی (Packaging Cost)

**وضعیت:** ❌ پیاده‌سازی نشده

**نیازمندی:**
- محاسبه هزینه بسته‌بندی بر اساس:
  - تعداد محصولات
  - نوع محصولات (شکننده، بزرگ، ...)
- افزودن فیلد `packagingCost` به Order

---

#### 9. استفاده از جزئیات ارسال

**وضعیت:** ❌ استفاده نمی‌شود

**مشکل:**
- `sendType`, `sendTime`, `sendDate`, `isBig` در `CheckoutMeta` وجود دارند
- اما در منطق استفاده نمی‌شوند

**نیازمندی:**
- ذخیره این اطلاعات در Order یا Package
- استفاده در محاسبه هزینه ارسال
- استفاده در مدیریت بسته

---

### 🟢 اولویت پایین

#### 10. تاریخچه تغییرات وضعیت سفارش

**وضعیت:** ❌ پیاده‌سازی نشده

**نیازمندی:**
- ثبت تاریخچه تغییرات وضعیت
- مدل `OrderStatusHistory`
- Repository برای تاریخچه

---

#### 11. اعتبارسنجی پیشرفته آدرس

**وضعیت:** ⚠️ ساده

**نیازمندی:**
- اعتبارسنجی کامل آدرس
- بررسی صحت کد پستی
- بررسی فاصله برای محاسبه هزینه ارسال

---

## اولویت‌بندی بهبودها

### فاز 2.1 (اولویت بالا - ضروری)

1. ✅ **محاسبه هزینه ارسال** - بدون این، checkout کامل نیست
2. ✅ **اعمال کد تخفیف** - کاربر باید بتواند کد تخفیف استفاده کند
3. ✅ **محاسبه مالیات** - برای فاکتور قانونی ضروری است
4. ✅ **شماره فاکتور** - برای رهگیری و مدیریت ضروری است
5. ✅ **محاسبه نهایی** - باید شامل همه هزینه‌ها باشد

### فاز 2.2 (اولویت متوسط)

6. ✅ **وضعیت سفارش** - برای مدیریت بهتر
7. ✅ **فاکتور/صورتحساب** - استفاده از InvoiceRepository موجود
8. ✅ **هزینه بسته‌بندی** - برای دقت بیشتر

### فاز 2.3 (اولویت پایین)

9. ✅ **تاریخچه تغییرات** - برای audit
10. ✅ **بهبود اعتبارسنجی** - برای دقت بیشتر

---

## پیشنهادات پیاده‌سازی

### 1. ساختار پیشنهادی برای Order Model

```typescript
interface Order {
  // ... فیلدهای موجود
  
  // فیلدهای جدید
  orderNumber?: string;           // شماره فاکتور
  orderStatus?: OrderStatus;       // وضعیت سفارش
  invoiceNumber?: string;          // شماره فاکتور مالی
  
  // محاسبات مالی
  totalPriceProducts: number;      // قیمت کل محصولات
  discountAmount?: number;          // مقدار تخفیف
  shippingCost?: number;           // هزینه ارسال
  taxAmount?: number;              // مالیات
  packagingCost?: number;          // هزینه بسته‌بندی
  finalTotal: number;              // مبلغ نهایی قابل پرداخت
  
  // جزئیات ارسال
  sendType?: number;               // نوع ارسال
  sendTime?: number;               // زمان ارسال
  sendDate?: number;               // تاریخ ارسال
  isBig?: boolean;                 // بسته بزرگ
  
  // تخفیف
  discountCode?: string;            // کد تخفیف استفاده شده
  
  // فاکتور
  invoice?: string | Invoice;      // اتصال به فاکتور
}
```

### 2. سرویس‌های پیشنهادی

#### ShippingService
```typescript
class ShippingService {
  calculateShippingCost(
    address: Address,
    sendType: number,
    sendTime: number,
    isBig: boolean,
    weight?: number
  ): number;
}
```

#### TaxService
```typescript
class TaxService {
  calculateTax(
    totalPrice: number,
    taxRate?: number
  ): number;
}
```

#### DiscountService
```typescript
class DiscountService {
  validateDiscountCode(code: string): Promise<Discount | null>;
  applyDiscount(
    totalPrice: number,
    discount: Discount
  ): { discountAmount: number; finalPrice: number };
}
```

### 3. جریان پیشنهادی Checkout

```
1. دریافت سبد کاربر
   ↓
2. اعتبارسنجی موجودی
   ↓
3. محاسبه قیمت کل محصولات
   ↓
4. اعمال کد تخفیف (در صورت وجود)
   ↓
5. محاسبه هزینه ارسال
   ↓
6. محاسبه مالیات
   ↓
7. محاسبه هزینه بسته‌بندی
   ↓
8. محاسبه مبلغ نهایی
   ↓
9. ثبت سفارش
   ↓
10. ایجاد فاکتور
   ↓
11. ایجاد پرداخت
   ↓
12. ایجاد بسته ارسالی
   ↓
13. بازگشت نتیجه کامل
```

---

## چک‌لیست پیاده‌سازی

### فاز 2.1 (اولویت بالا)

- [ ] ایجاد `ShippingService` برای محاسبه هزینه ارسال
- [ ] افزودن فیلد `shippingCost` به Order
- [ ] افزودن فیلد `orderNumber` به Order
- [ ] ایجاد `TaxService` برای محاسبه مالیات
- [ ] افزودن فیلد `taxAmount` به Order
- [ ] ایجاد `DiscountService` برای اعمال کد تخفیف
- [ ] افزودن فیلد `discountAmount` و `discountCode` به Order
- [ ] افزودن فیلد `finalTotal` به Order
- [ ] به‌روزرسانی `BasketOrderService` برای محاسبه همه هزینه‌ها

### فاز 2.2 (اولویت متوسط)

- [ ] افزودن فیلد `orderStatus` به Order
- [ ] ایجاد `OrderStatusService` برای مدیریت وضعیت
- [ ] اتصال Order به Invoice
- [ ] استفاده از `InvoiceRepository` برای ایجاد فاکتور
- [ ] افزودن فیلد `packagingCost` به Order
- [ ] ذخیره جزئیات ارسال در Order

### فاز 2.3 (اولویت پایین)

- [ ] ایجاد مدل `OrderStatusHistory`
- [ ] ثبت تاریخچه تغییرات وضعیت
- [ ] بهبود اعتبارسنجی آدرس

---

## نکات مهم

1. **محاسبه نهایی:** باید شامل همه هزینه‌ها و تخفیف‌ها باشد
2. **فاکتور:** باید پس از ثبت سفارش ایجاد شود
3. **کد تخفیف:** باید در checkout اعمال شود، نه فقط پس از فاکتور
4. **هزینه ارسال:** باید بر اساس نوع و زمان ارسال محاسبه شود
5. **مالیات:** برای فاکتور قانونی ضروری است

---

**توسعه‌دهندگان:** Hasan Mohammadi, Mahtdy  
**نسخه:** 1.0.0

